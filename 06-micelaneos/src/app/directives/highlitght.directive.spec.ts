import { HighlitghtDirective } from './highlitght.directive';

describe('HighlitghtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlitghtDirective();
    expect(directive).toBeTruthy();
  });
});
