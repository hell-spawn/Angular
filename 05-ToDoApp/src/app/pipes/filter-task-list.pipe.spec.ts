import { FilterTaskListPipe } from './filter-task-list.pipe';

describe('FilterTaskListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTaskListPipe();
    expect(pipe).toBeTruthy();
  });
});
